import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.actiondirecte.app',
  appName: 'Action Directe',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
