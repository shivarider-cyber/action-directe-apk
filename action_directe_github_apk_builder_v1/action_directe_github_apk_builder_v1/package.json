{
  "name": "action-directe-github-apk",
  "version": "1.0.0",
  "private": true,
  "description": "Action Directe APK builder via GitHub Actions",
  "scripts": {
    "sync": "npx cap sync android",
    "open": "npx cap open android",
    "build:android": "npx cap add android && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}